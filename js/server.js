// var serverHost = "devu";
var serverHost = "na";

/*
function getEnvironment(hostname) {
	alert(hostname);
    if (hostname.match("^localhost")) {​
        return "local";​
    } else {​
        return hostname.match(/^help-us\.(\S+)\.smart-integration.ricoh.com/)[1];​
    }​
}
serverHost = getEnvironment(window.location.host);
*/

//alert(serverHost);
